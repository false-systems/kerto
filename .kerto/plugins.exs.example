# Kerto plugin configuration
# Copy this file to plugins.exs and uncomment the plugins you want:
#
# cp .kerto/plugins.exs.example .kerto/plugins.exs
#
# Each entry is a module implementing the Kerto.Plugin behaviour
# (agent_name/0 and scan/1 callbacks — see lib/kerto/plugin/plugin.ex).

[
  # Kerto.Plugin.Claude,
  # Kerto.Plugin.Logs,
]
